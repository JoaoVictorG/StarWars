import React from 'react';
import ReactDOM from 'react-dom/client';
import List from './pages/listResults/index'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <List />
  </React.StrictMode>
);
